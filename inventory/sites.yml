# Site Registry -- Generated by deploy_configure.py
# Re-run the wrapper to add or modify sites.
#
# Usage: python3 scripts/fleet_inventory.py validate

valid_roles:
- dc
- sql
- iis
- fileserver
- docker
- generic
valid_os:
- windows
- linux
sites:
  ent:
    display_name: Enterprise HQ
    timezone: America/Denver
    environment: prod
    contact_email: ent-ops@example.com
    network_segment: 10.0.0.0/16
    gateway:
      enabled: true
      snmp_targets: true
      redfish_targets: true
      cert_targets: true
  dv:
    display_name: Deer Valley
    timezone: America/Denver
    environment: prod
    contact_email: dv-ops@example.com
    network_segment: 10.1.0.0/16
    gateway:
      enabled: true
      snmp_targets: true
      redfish_targets: true
      cert_targets: true
